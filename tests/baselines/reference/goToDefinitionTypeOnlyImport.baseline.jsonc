// === /a.ts ===
// enum [|SyntaxKind|] { SourceFile }
// export type { SyntaxKind }

// === /c.ts ===
// import type { SyntaxKind } from './b';
// let kind: /*GOTO DEF*/SyntaxKind;

// === Definitions ===
  // === 0 ===
    // kind: "enum"
    // name: "SyntaxKind"
    // containerName: ""
    // isLocal: true
    // isAmbient: false
    // unverified: false
    // === /a.ts ===
    // enum [|SyntaxKind|] { SourceFile }
    // export type { SyntaxKind }
      // === ContextSpan ===
      // === /a.ts ===
      // [|enum SyntaxKind { SourceFile }|]
      // export type { SyntaxKind }
// === TextSpan ===
  // === /c.ts ===
  // import type { SyntaxKind } from './b';
  // let kind: [|SyntaxKind|];


{
  "definitions": [
    {
      "fileName": "/a.ts",
      "textSpan": {
        "start": 5,
        "length": 10
      },
      "kind": "enum",
      "name": "SyntaxKind",
      "containerName": "",
      "contextSpan": {
        "start": 0,
        "length": 30
      },
      "isLocal": true,
      "isAmbient": false,
      "unverified": false
    }
  ],
  "textSpan": {
    "start": 49,
    "length": 10
  }
}